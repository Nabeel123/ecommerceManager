# Migration `20201228090549-catalog-product-with-multiple-images`

This migration has been generated by Sohail Haider at 12/28/2020, 2:05:49 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `CatalogProduct` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `title` VARCHAR(191) NOT NULL,
    `regularPrice` DECIMAL(65,30),
    `salePrice` DECIMAL(65,30),
    `description` VARCHAR(191),
    `discount` DECIMAL(65,30),
    `weight` DECIMAL(65,30),
    `weightUnit` ENUM('KG', 'POUND', 'GM'),
    `shortDesc` VARCHAR(191),

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `Image` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `title` VARCHAR(191),
    `url` VARCHAR(191),
    `catalogProductId` INT,

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `Image` ADD FOREIGN KEY (`catalogProductId`) REFERENCES `CatalogProduct`(`id`) ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201228085901-catalog-product-new-added..20201228090549-catalog-product-with-multiple-images
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -49,8 +49,21 @@
   catalog Catalog
   userId    Int
 }
+model CatalogProduct {
+  id            Int      @default(autoincrement()) @id
+  title         String
+  regularPrice  Float?
+  salePrice     Float?
+  description   String?
+  discount      Float?
+  weight        Float?
+  weightUnit    WeightUnit?
+  shortDesc     String?
+  images        Image[]
+}
+
 model Category {
   id    Int     @id @default(autoincrement())
   name  String
   products Product[]
@@ -73,5 +86,11 @@
 enum WeightUnit {
   KG
   POUND
   GM
+}
+
+model Image {
+  id        Int       @default(autoincrement()) @id
+  title     String?
+  url       String?
 }
```


