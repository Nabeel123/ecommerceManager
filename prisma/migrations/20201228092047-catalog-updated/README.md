# Migration `20201228092047-catalog-updated`

This migration has been generated by Sohail Haider at 12/28/2020, 2:20:47 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `Catalog` ADD COLUMN     `image` VARCHAR(191),
    ADD COLUMN     `userId` INT

ALTER TABLE `CatalogProduct` ADD COLUMN     `userId` INT NOT NULL
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201228091210-switch-prodcts-to-catalog-products---new-product-db..20201228092047-catalog-updated
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -37,18 +37,18 @@
   updatedAt DateTime  @updatedAt
 }
 model Product {
-  id    Int      @default(autoincrement()) @id
-  name  String
-  image String
-  price Float
+  id          Int      @default(autoincrement()) @id
+  name        String
+  image       String
+  price       Float
   description String
-  status  Status
-  categories Category[]
+  status      Status
+  categories  Category[]
   wordpressStore  WordpressStore?
-  catalog Catalog
-  userId    Int
+  catalog     Catalog
+  userId      Int
 }
 model CatalogProduct {
   id            Int      @default(autoincrement()) @id
@@ -61,8 +61,9 @@
   weightUnit    WeightUnit?
   shortDesc     String?
   images        Image[]
   catalog       Catalog?
+  userId        Int
 }
 model Category {
   id    Int     @id @default(autoincrement())
@@ -70,12 +71,14 @@
   products Product[]
 }
 model Catalog {
-  id    Int     @id @default(autoincrement())
-  name  String
-  products  CatalogProduct[]
-  status  Status
+  id            Int     @id @default(autoincrement())
+  name          String
+  products      CatalogProduct[]
+  status        Status
+  image         String?
+  userId        Int?
 }
 enum Status {
   PENDING
```


